# Forkity architecture diagrams

- `current-state.png` shows the existing frontend-only application flow.
- `target-state.png` shows the planned frontend, backend, authentication, and database boundaries.

The editable Mermaid sources are stored beside the PNG files as `.mmd` files.